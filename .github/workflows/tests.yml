name: tests

on:
  push:
    branches:
      - main
      - agent/product-master-catalog
  pull_request:

permissions:
  contents: write

jobs:
  unittest:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Apply Station Light matching patch
        run: PYTHONPATH=. python scripts/apply_station_light_matching_fix.py

      - name: Run tests
        shell: bash
        run: |
          set +e
          python -m unittest discover -s tests > test-output.txt 2>&1
          status=$?
          tail -n 200 test-output.txt
          exit $status

      - name: Commit generated production patch
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/services/model_code_matching.py app/services/variant_matching.py app/services/price_service.py
          if git diff --cached --quiet; then
            echo "Patch already applied"
            exit 0
          fi
          git commit -m "Fix code-prefix brands and clock variants"
          git push origin HEAD:${{ github.head_ref }}
